#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _memset - function that fills memory with a constant byte
 * @s: pointer variable
 * @b: variable
 * @n: size
 * Return: s Always
 */
char *_memset(char *s, char b, unsigned int n)
{
	memset(s, b, n)
		return (s);
}
